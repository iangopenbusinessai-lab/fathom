---
name: fathom-assets
description: PWA manifest, service worker, runtime <head> injection, and app/favicon icon regeneration for Fathom. Use when touching public/manifest.json, public/sw.js, the icon SVGs or PNGs, or anything injected into <head> at runtime from main.tsx.
---

# Fathom assets: PWA plumbing and app icons

## PWA / runtime `<head>` injection
index.html is finalized, so everything that would normally be a tag in <head>
is injected at runtime from main.tsx instead. src/lib/favicon.ts is the original
of the pattern; title.ts and manifest.ts follow it.

- public/manifest.json - name/short_name Fathom, standalone, theme and
  background #0f172a. Every path in it (start_url, scope, icons) is RELATIVE, so
  it resolves against the manifest's own URL and picks up the /fathom/ base
  without hardcoding it. Do not make these absolute.
- public/sw.js - deliberately minimal: a pass-through fetch handler and nothing
  else, present only because installability requires a service worker. There is
  NO offline caching by design; the app still needs a connection to load.
- src/lib/serviceWorker.ts registers it, and skips registration unless
  import.meta.env.PROD - use `npm run preview` to exercise install behaviour
  locally, not `npm run dev`.
- src/vite-env.d.ts exists only to type import.meta.env. tsconfig.json has no
  "include", so it is picked up automatically.

## App icons
The mark is a six-spindle helm with a stencil-cut F at its centre - the F
broken by two stencil bridges, which is what ties it to the FATHOM wordmark
rather than leaving the two merely adjacent. It came from the "Stencil-cut F"
handoff in the Fathom logo exploration (Claude Design). The lead-line mark it
replaced, and the depth-rings concept beside it, are in git history.

- public/icon-wheel.svg is the shipped app icon: brass mark on a navy plate,
  11% padding. public/icon-maskable.svg is the same artwork inside a 19% safe
  zone for launcher masking.
- public/favicon.svg carries the same mark with the stencil bridges REMOVED.
  The bridges are 3.2 units - about half a pixel at 16px - so they fill in and
  muddy the stem at tab sizes. Two files, one shape: keep them that way, and if
  the mark ever changes, change both, because the tab and the installed app
  must not read as two different products.
- The masthead draws the bridged mark inline in ChartFrame, in `currentColor`
  with `color: var(--ct-brass)`, so one copy follows the theme.
- PNGs (icon-192, icon-512, icon-maskable-512) are generated by
  `node scripts/generate-icons.mjs [icon-name]`, which shells out to
  `npx sharp-cli` so no rasteriser is a project dependency. The build does not
  run it - regenerate and commit the PNGs whenever an icon SVG changes.
- Known nit, not yet fixed: the icon plates are #0a1929 (the app's --ct-bg)
  while manifest.json's theme_color and background_color are #0f172a. On an
  Android splash the two grounds differ very slightly.
